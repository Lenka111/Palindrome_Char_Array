# Palindrome_charArray
Program that checks if a word is a palindrome or not.
Reverse a string using Char Array.
Traverse the array in reverse.
Compare the reversed word to the user's input initial word.
